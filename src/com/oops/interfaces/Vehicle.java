package com.oops.interfaces;

//interface keyword being use to tell this is interface
interface Vehicle {
      
    // all are the abstract methods.
    void changeGear(int a);
    void speedUp(int a);
    void applyBrakes(int a);

    //we can say interface as the full abstact class
    //here we just show thing need to show to customer/user
}